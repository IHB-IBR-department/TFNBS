"""Bundled atlas / parcellation resources.

CSV files in this directory are loaded by :mod:`conninfpy.atlas` via
``importlib.resources``. They are kept here (rather than in the
top-level ``datasets/`` tree) so they are installed alongside the
package when users ``pip install conninfpy``.
"""
